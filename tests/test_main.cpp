
// ----------------------------------------------------------------------------
// doctest entry point. Linked into every test executable so the test source
// files themselves only `#include <doctest/doctest.h>` without the main macro.
// ----------------------------------------------------------------------------

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
